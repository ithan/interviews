---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'header'> {}

const { class: className, ...rest } = Astro.props;
---

<header class:list={['card__header', className]} data-slot="card-header" {...rest}>
  <slot />
</header>

