---
import type { HTMLAttributes } from 'astro/types';

interface Props extends HTMLAttributes<'footer'> {}

const { class: className, ...rest } = Astro.props;
---

<footer class:list={['card__footer', className]} data-slot="card-footer" {...rest}>
  <slot />
</footer>


